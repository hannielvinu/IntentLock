# Leave unset to use the zero-setup local JSON store.
# Set to a local or managed PostgreSQL connection string to enable PostgreSQL storage.
DATABASE_URL=postgresql://intentlock:intentlock_local@localhost:5432/intentlock
